name: Cron Job

on:
    schedule:
        - cron: ''
    workflow_dispatch:

jobs:
    run-bun:
        runs-on: ubuntu-latest
        env:
            // MEALPAL_USERNAME: ${{ secrets.MEALPAL_USERNAMEE }}
            // MEALPAL_PASSWORD: ${{ secrets.MEALPAL_PASSWORDD }}
        steps:
            - uses: oven-sh/setup-bun@v1
              with:
                  bun-version: latest

            - uses: actions/checkout@v3

            - name: Install deps
              run: bun install

            - name: Run script
              run: bun index.ts
